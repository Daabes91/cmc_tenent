{
  "name": "api",
  "version": "1.0.0",
  "description": "Spring Boot service exposing: - ` /public/**` for patient-facing endpoints. - ` /admin/**` for staff-only endpoints (RBAC enforced).",
  "main": "generate-jwt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
